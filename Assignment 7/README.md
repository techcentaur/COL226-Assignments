## Toy Interpreter Prolog

Read the statement of this assignment [here](./Statement.txt)

## Usage 

`lexer.mll` is lexer and `parser.mli` is parser.

use `make` to run, and `make clean` to clean the setup.